node_modules
# Keep environment variables out of version control
.env
.env*

/src/generated/prisma

# tsc output — built inside the image, never committed
/dist

# Uploaded files (avatars) — served from disk, not committed
/uploads

